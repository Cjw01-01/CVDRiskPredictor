[build]
  command = "npm run copy-models && cd frontend && npm install && npm run build"
  publish = "frontend/build"

[functions]
  directory = "netlify/functions"
  included_files = [
    "backend/**",
    "netlify/functions/**"
  ]

# Redirect /api/predict to function (functions handle their own paths)
[[redirects]]
  from = "/api/predict"
  to = "/.netlify/functions/predict"
  status = 200
  force = true

# Catch-all for frontend routes (must be last)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

